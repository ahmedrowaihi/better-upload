<section id="s3-clients">

```ts tab="AWS S3"
import { aws } from '@ahmedrowaihi/better-upload-server/clients';

const s3 = aws({
  accessKeyId: 'your-access-key-id',
  secretAccessKey: 'your-secret-access-key',
  region: 'us-east-1',
});
```

```ts tab="Cloudflare R2"
import { cloudflare } from '@ahmedrowaihi/better-upload-server/clients';

const s3 = cloudflare({
  accountId: 'your-account-id',
  accessKeyId: 'your-access-key-id',
  secretAccessKey: 'your-secret-access-key',
});
```

```ts tab="Tigris"
import { tigris } from '@ahmedrowaihi/better-upload-server/clients';

const s3 = tigris({
  accessKeyId: 'your-access-key-id',
  secretAccessKey: 'your-secret-access-key',
  endpoint: '...', // optional
});
```

```ts tab="Backblaze B2"
import { backblaze } from '@ahmedrowaihi/better-upload-server/clients';

const s3 = backblaze({
  region: 'your-backblaze-region',
  applicationKeyId: 'your-application-key-id',
  applicationKey: 'your-application-key',
});
```

```ts tab="DigitalOcean Spaces"
import { digitalOcean } from '@ahmedrowaihi/better-upload-server/clients';

const s3 = digitalOcean({
  region: 'your-spaces-region',
  key: 'your-spaces-key',
  secret: 'your-spaces-secret',
});
```

```ts tab="Wasabi"
import { wasabi } from '@ahmedrowaihi/better-upload-server/clients';

const s3 = wasabi({
  region: 'your-wasabi-region',
  accessKeyId: 'your-access-key-id',
  secretAccessKey: 'your-secret-access-key',
});
```

```ts tab="MinIO"
import { minio } from '@ahmedrowaihi/better-upload-server/clients';

const s3 = minio({
  region: 'your-minio-region',
  endpoint: 'https://minio.example.com',
  accessKeyId: 'your-access-key-id',
  secretAccessKey: 'your-secret-access-key',
});
```

```ts tab="Linode"
import { linode } from '@ahmedrowaihi/better-upload-server/clients';

const s3 = linode({
  region: 'your-linode-region',
  accessKey: 'your-access-key',
  secretKey: 'your-secret-key',
});
```

```ts tab="Custom"
// for any S3-compatible service
import { custom } from '@ahmedrowaihi/better-upload-server/clients';

const s3 = custom({
  host: 's3.us-east-1.amazonaws.com',
  accessKeyId: 'your-access-key-id',
  secretAccessKey: 'your-secret-access-key',
  region: 'us-east-1',
  secure: true,
  forcePathStyle: false,
});
```

</section>
